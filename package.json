{
  "name": "serviceops-analytics-dashboard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate-demo": "node scripts/generate-demo-data.mjs",
    "validate": "node scripts/validate-demo-data.mjs",
    "refresh-demo": "npm run generate-demo && npm run validate",
    "predev": "npm run refresh-demo",
    "dev": "vite --open",
    "prebuild": "npm run refresh-demo",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@fontsource-variable/inter": "^5.2.8",
    "chart.js": "^4.5.0"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
